name: Backtest

# The reference month runs in CI, so the published result is continuously
# verified rather than quoted from a run nobody can afford to repeat. This
# workflow exists for running a different period or asset on demand.

on:
  workflow_dispatch:
    inputs:
      start:
        description: Inclusive physical period boundary
        required: true
        default: "2022-11-01 00:00"
      end:
        description: Exclusive physical period boundary
        required: true
        default: "2022-12-01 00:00"
      degradation_cost:
        description: AUD per discharged MWh
        required: true
        default: "25"

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install uv
      - run: uv sync --frozen
      - run: >-
          uv run battery-dispatch
          --start "${{ inputs.start }}"
          --end "${{ inputs.end }}"
          --region SA1
          --connection-point "Hornsdale 275 kV"
          --loss-factor-file config/loss_factors.csv
          --degradation-cost "${{ inputs.degradation_cost }}"
      - uses: actions/upload-artifact@v4
        with:
          name: backtest
          path: outputs/
